import { ScrollToViewDirective } from './scroll-to-view.directive';

describe('ScrollToViewDirective', () => {
  it('should create an instance', () => {
    const directive = new ScrollToViewDirective();
    expect(directive).toBeTruthy();
  });
});
