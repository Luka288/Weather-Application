import { TrackWidthDirective } from './track-width.directive';

describe('TrackWidthDirective', () => {
  it('should create an instance', () => {
    const directive = new TrackWidthDirective();
    expect(directive).toBeTruthy();
  });
});
